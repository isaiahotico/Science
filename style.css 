ss
/* ... (style.ss content remains the same as before) ... */

body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    margin: 0;
    padding: 0;
    /* Pure gold shining bright everytime */
    background-color: #FFD700; /* Gold */
    background: linear-gradient(135deg, #FFD700 0%, #FFC107 50%, #FFD700 100%);
    animation: goldShine 5s infinite linear;
    color: #333;
    display: flex;
    justify-content: center;
    align-items: flex-start;
    min-height: 100vh;
    padding: 20px;
    box-sizing: border-box;
}

@keyframes goldShine {
    0% { background-position: 0% 50%; }
    50% { background-position: 100% 50%; }
    100% { background-position: 0% 50%; }
}


.app-container {
    background-color: #fff;
    border-radius: 10px;
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
    max-width: 900px;
    width: 100%;
    overflow: hidden;
    padding: 25px;
}

header {
    text-align: center;
    padding-bottom: 20px;
    border-bottom: 1px solid #eee;
    margin-bottom: 25px;
    position: relative;
}

.app-title {
    color: #c62828; /* Darker red */
    margin: 0;
    font-size: 2.2em;
    font-weight: 700;
    text-shadow: 1px 1px 2px rgba(0,0,0,0.2); /* Added for depth */
}

.currency-display {
    position: absolute;
    top: 25px;
    right: 25px;
    font-size: 1.4em;
    font-weight: bold;
    color: #4CAF50; /* Green for currency */
    text-shadow: 0 0 5px rgba(76, 175, 80, 0.7); /* Shining effect for peso balance */
}

.telegram-info {
    font-size: 0.9em;
    color: #666;
    margin-top: 10px;
}

.main-nav {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 12px;
    margin-bottom: 35px;
}

.nav-button, .action-button, .home-button {
    background-color: #e53935; /* Primary app red */
    color: white;
    border: none;
    padding: 12px 18px;
    border-radius: 8px;
    cursor: pointer;
    font-size: 0.95em;
    font-weight: 600;
    transition: background-color 0.3s ease, transform 0.2s ease;
    flex-grow: 1;
    max-width: 180px;
    box-shadow: 0 2px 5px rgba(0,0,0,0.1);
}

.nav-button:hover, .action-button:hover, .home-button:hover {
    background-color: #c62828;
    transform: translateY(-2px);
}

.nav-button:disabled, .action-button:disabled {
    background-color: #cccccc;
    cursor: not-allowed;
    box-shadow: none;
    transform: none;
}

.admin-button {
    background-color: #3f51b5; /* Blue for admin */
}
.admin-button:hover {
    background-color: #303f9f;
}

.page {
    padding: 25px;
    border: 1px solid #e0e0e0;
    border-radius: 10px;
    background-color: #fcfcfc;
    margin-bottom: 25px;
    box-shadow: inset 0 1px 3px rgba(0,0,0,0.05);
}

.hidden {
    display: none;
}

h2, h3 {
    color: #e53935;
    margin-top: 0;
    margin-bottom: 20px;
    border-bottom: 1px solid #f0f0f0;
    padding-bottom: 10px;
}

input[type="text"],
input[type="number"],
input[type="email"],
input[type="password"] {
    width: calc(100% - 22px);
    padding: 12px;
    margin-bottom: 15px;
    border: 1px solid #ccc;
    border-radius: 6px;
    font-size: 1em;
    box-shadow: inset 0 1px 2px rgba(0,0,0,0.05);
}

table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 20px;
}

table, th, td {
    border: 1px solid #e0e0e0;
}

th, td {
    padding: 10px 12px;
    text-align: left;
    vertical-align: top;
}

th {
    background-color: #f5f5f5;
    font-weight: 700;
    color: #555;
}

td a {
    color: #3f51b5;
    text-decoration: none;
}
td a:hover {
    text-decoration: underline;
}

#player {
    width: 100%;
    aspect-ratio: 16 / 9; /* For responsive YouTube player */
    background-color: #000;
    margin-bottom: 15px;
    border-radius: 8px;
    overflow: hidden; /* To keep the video within the rounded corners */
}

.youtube-controls {
    display: flex;
    gap: 12px;
    margin-top: 20px;
}

.home-button {
    margin-top: 30px;
    background-color: #607d8b; /* Greyish blue */
}
.home-button:hover {
    background-color: #455a64;
}

/* Specific styles for withdrawal status */
.status-Pending { color: orange; font-weight: 600; }
.status-Approved { color: green; font-weight: 600; }
.status-Rejected, .status-Failed { color: red; font-weight: 600; }

/* Leaderboard Specifics */
#topEarnersTable tbody tr:nth-child(1) td,
#topVideosTable tbody tr:nth-child(1) td {
    background-color: #ffeb3b4d; /* Light gold for rank 1 */
    font-weight: bold;
}
#topEarnersTable tbody tr:nth-child(2) td,
#topVideosTable tbody tr:nth-child(2) td {
    background-color: #bdbdbd4d; /* Light silver for rank 2 */
    font-weight: bold;
}
#topEarnersTable tbody tr:nth-child(3) td,
#topVideosTable tbody tr:nth-child(3) td {
    background-color: #ffab914d; /* Light bronze for rank 3 */
    font-weight: bold;
}

/* Timer for reward */
#rewardTimer {
    margin-top: 15px;
    padding: 10px;
    background-color: #ffe0b2; /* Light orange background */
    border: 1px solid #ffb74d;
    border-radius: 5px;
    font-weight: bold;
    color: #d84315; /* Dark orange text */
    text-align: center;
}

/* Responsive adjustments */
@media (max-width: 768px) {
    .main-nav {
        flex-direction: column;
        align-items: stretch;
    }
    .nav-button {
        max-width: 100%;
    }
    .app-container {
        padding: 15px;
    }
    header {
        position: static;
        padding-bottom: 15px;
    }
    .currency-display {
        position: static;
        margin-top: 10px;
        text-align: center;
    }
    .telegram-info {
        text-align: center;
    }
    h2, h3 {
        text-align: center;
    }
    th, td {
        padding: 8px;
        font-size: 0.9em;
    }
    #player {
        height: auto; /* Allow aspect-ratio to control height */
    }
}
@media (max-width: 480px) {
    .app-title {
        font-size: 1.8em;
    }
    .currency-display {
        font-size: 1.2em;
    }
    .nav-button, .action-button, .home-button {
        padding: 10px 12px;
        font-size: 0.85em;
    }
    input {
        padding: 10px;
    }
}
